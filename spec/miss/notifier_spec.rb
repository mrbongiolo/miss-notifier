require 'spec_helper'

describe Miss::Notifier do
  it 'has a version number' do
    expect(Miss::Notifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
